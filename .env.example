# Optional. Without it the intake still works, but falls back to
# rule-based extraction instead of the model.
ANTHROPIC_API_KEY=

# Optional override for the SQLite file location.
# DISPECR_DB=./data/dispecr.db
